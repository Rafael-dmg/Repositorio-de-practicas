let a = 1;
let c = 3;
let b = c;


if(a < b) {
    console.log('a is smaller than b')
    return 1
}
let height=180;

console.log(height);
console.log(weight)
//para poder abrir el interprete de JS debemos hacer apretar ctrl+p, escribir '>' y luego darle a la opcion abrir current file de quokka o start a new file//