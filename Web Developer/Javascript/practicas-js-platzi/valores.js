Valores primitivos

Valor numérico
si queremos utilizar valores numéricos, lo escribimos de forma común y ese valor seria un valor tipo número

Valores de texto
Si queremos usar valores de texto, o cadenas de texto (string), la forma de hacerlo es ocupando comillas, es decir, colocando comillas al principio y al final de cada línea de texto. Es la única manera de decirle a Javascript que queremos genera un valor que queramos utilizar o llamar después para que el usuario lo pueda utilizar, leer o generar una accion después.

Valor booliano
ese valor lo guardamos con TRUE (1) o FALSE (0). Se utilizan para validar si tomamos una decisión o no.

Valores vacíos, valores placeholder, o valores reservados 
NULL y UNDEFINED son valores reservados para cuando queda algún valor faltante. Cuando tenemos un valor faltante debemos ocupar ese valor en la memoria con NULL o UNDEFINED podemos reservar un espacio para cuando tengamos ese valor. Debemos evitar UNDEFINED

Valores tipo objetos

Tipo arraial

Este se utiliza usando corchetes. Normalmente se utilizan valores primitivos dentro de los corchetes casi siempre. Esto genera el arraial

Valor tipo objeto
Se crea utilizando llaves.